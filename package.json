{
  "name": "eworker360-site",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev",
    "check:deploy": "wrangler deploy --dry-run",
    "test:assets": "node tests/cloudflare-runtime.js"
  },
  "devDependencies": {
    "ignore": "7.0.8",
    "wrangler": "4.129.0"
  }
}
